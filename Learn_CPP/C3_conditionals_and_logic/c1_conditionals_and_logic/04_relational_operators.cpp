// Same old but rinse repeat to try make it stick!
//
// == equal to
// != not equal to
// > greater than
// < lesser than
// >= greater than or equal to
// >= lesser than or equal to

// Relational operators compare the value on the left with the value on the right. 


#include <iostream>
using namespace std;

int main() {
  int grade = 90;

  if (grade < 60) {
    cout << "Pass\n";
  }
  else if (grade != 60) {
    cout << "Pass\n";
  }
}

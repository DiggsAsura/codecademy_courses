/* Numeric Operations
 *
 * Rust supports the basic stuff you'd expect. */

fn main() {
    // addition
    let sum = 4 + 3;

    // subtraction
    let difference = 95.4 - 23.3;

    // multiplication
    let product = 3 * 29;

    // division
    let quotient = 45.7 / 32.2;
    let floored = 2 / 3; // Results in 0

    // remainder / modulo
    let remainder = 23 % 3;
}

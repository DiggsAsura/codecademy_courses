fn main() {
    let name = "kenneth";

    if let "kenneth" = name {
        println!("{name}");
    }
    let number = Some(9);

    if let Some(i) = number {
        println!("{i}");
    }

    /* I understand but don't understande i guess. */
}

/* The ref pattern
 * ================
 *
 * When doing pattern matching or destructuring via the let binding, the ref keyword can be used to
 * take referencews
fn main() {
    println!("Hello, world!");
}
